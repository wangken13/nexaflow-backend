server:
  port: 8103
spring:
  application:
    name: customer-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:192.168.101.1:8848}
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/trade_ai?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USER:trade}
    password: ${MYSQL_PASSWORD:trade123}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672
    username: ${RABBITMQ_USER:trade}
    password: ${RABBITMQ_PASSWORD:trade123}
